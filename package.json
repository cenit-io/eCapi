{
  "name": "omna-v2-docs",
  "version": "1.0.0",
  "dependencies": {
    "docsify": "^4.11.3",
    "swagger-repo": "^2.0.0-rc.15"
  },
  "private": true,
  "scripts": {
    "start": "swagger-repo serve",
    "build": "node bin/cli build-docs && swagger-repo build -o web_deploy/api-spec",
    "gh-pages": "swagger-repo gh-pages --clean",
    "test": "swagger-repo validate"
  }
}
