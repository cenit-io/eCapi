name: category_id
in: query
schema:
  description: |
    It allows to filter items by the category of an integration.

    > This filter parameter is only supported in conjunction with the filter by integration.
  type: string
  example: 1532