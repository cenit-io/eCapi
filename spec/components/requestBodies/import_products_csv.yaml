description: CSV content of data to be imported.
required: true
content:
  application/json:
    schema:
        properties:
            csv:
                description: CSV raw text.
                type : string
                
            
    examples:
      Ex1:
        $ref: '#/components/examples/import_products_request'