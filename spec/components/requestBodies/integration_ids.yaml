required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          properties:
            integration_ids:
              description: Identifiers of the integrations to be processing.
              type: array
              minItems: 1
              maxItems: 100
              items:
                type: string
          required: [integration_ids]
