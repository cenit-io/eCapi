required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          allOf:
            - $ref: "#/components/schemas/ProductLinkVariants"