description: |
  Data to set whether the product should be removed from the remote integration source.
required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          properties:
            delete_from_integration:
              description: |
                Sets whether the product should be removed from the remote integration source.
              type: boolean
              default: false

    examples:
      Ex1:
        $ref: '#/components/examples/integration_product_unlink_request'