required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          properties:
            variant_ids:
              description: Identifiers of the variants to be processing.
              type: array
              minItems: 1
              maxItems: 100
              items:
                type: string
          required: [variant_ids]
