description: |
  Identifiers of the variants and integrations that will be linked.
required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          allOf:
            - $ref: "#/components/schemas/VariantIDs"
            - $ref: "#/components/schemas/IntegrationIDs"