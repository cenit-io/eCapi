description: Variant properties that will be updated
required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          required: [properties]
          properties:
            properties:
              type: array
              items:
                $ref: "#/components/schemas/VariantProperty"