description: |
  Integrations IDs and data to set whether the variant should be removed from the remote integration source.
required: true
content:
  application/json:
    schema:
      required: [data]
      properties:
        data:
          type: object
          allOf:
            - $ref: "#/components/schemas/ULinks"