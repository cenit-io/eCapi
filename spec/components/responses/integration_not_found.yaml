description: Not found
content:
  application/json:
    schema:
      $ref: '#/components/schemas/Exception'
    example:
      type: 'exception'
      message: "Not found integration with id: shop_01"