description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/OrderDocument'

    example:
      $ref: '#/components/examples/order_doc_response'