description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/Product'

    examples:
      Ex1:
        $ref: '#/components/examples/product_item_response'