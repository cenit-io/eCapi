description: |
  Redirect to given URI.

  > Returns to the application using the URI given in the redirect_uri parameter.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/User'

    examples:
      Ex1:
        $ref: '#/components/examples/user_item_response'