description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/Webhook'

    example:
      $ref: '#/components/examples/webhook_item_response'