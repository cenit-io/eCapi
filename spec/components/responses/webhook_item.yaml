description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          $ref: '#/components/schemas/Webhook'

    examples:
      Ex1:
        $ref: '#/components/examples/webhook_item_response'