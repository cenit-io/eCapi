type: object
required: [integration_ids]
properties:
  integration_ids:
    description: Identifiers of the integations to be processed.
    type: array
    minItems: 1
    maxItems: 100
    items:
      type: string