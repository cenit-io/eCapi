type: object
required: [variant_ids]
properties:
  variant_ids:
    description: Identifiers of the product variants to be processed.
    type: array
    minItems: 1
    maxItems: 100
    items:
      type: string