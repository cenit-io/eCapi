parameters:
  - $ref: '#/components/parameters/path_integration_id'
get:
  tags:
    - Products
  operationId: import_products
  summary: IMPORT UPDATED PRODUCTS FROM INTEGRATION
  description: |
    Import the last updated products from an integration
  responses:
    '200':
      $ref: '#/components/responses/task_item'
    '404':
      $ref: '#/components/responses/integration_not_found'
