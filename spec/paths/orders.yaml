parameters:
  - $ref: '#/components/parameters/filter_term_for_orders'
  - $ref: '#/components/parameters/filter_store'
  - $ref: '#/components/parameters/offset'
  - $ref: '#/components/parameters/limit'

get:
  tags:
    - Order
  operationId: getOrders
  summary: Get orders
  description: |
    Returns the list of orders.
  responses:
    200:
      $ref: '#/components/responses/order_items'