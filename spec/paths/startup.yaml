get:
  tags:
    - Users
  operationId: startup_tenant
  summary: Startup
  description: |
    Generate an async task to startup OMNA Commerce in the authenticated tenant.

    > It is not necessary to call this service when the tenant has been created with the application or api of OMAN.
  responses:
    '200':
      $ref: '#/components/responses/startup_task'