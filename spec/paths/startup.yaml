get:
  tags:
    - Users
  operationId: startup_tenant_async_
  summary: Startup
  description: |
    Generate an async task to startup the platform in the authenticated tenant.

    > It is not necessary to call this service when the tenant has been created with the platform application or api.
  responses:
    '200':
      $ref: '#/components/responses/startup_task'