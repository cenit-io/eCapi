delete:
  tags:
    - Tasks
  operationId: delete_task_bulk_beta_
  summary: Task Delete Bulk
  description: |
    Find the tasks for the given id list and delete them

    > A running task can't be deleted.

  requestBody:
    $ref: '#/components/requestBodies/task_id_list'
  responses:
    '200':
      $ref: '#/components/responses/success'
    '404':
      $ref: '#/components/responses/task_not_found'
